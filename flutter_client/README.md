# EARTH Flutter client

This is the shared client foundation for Web, iOS, and macOS. It reads canonical state from the local/server-authoritative API rather than calculating balances or market outcomes locally.

```bash
flutter pub get
flutter run -d chrome
```

The selected futuristic product UI remains in the Prototype 3 web client while the Flutter client grows into the shared multi-platform application described in the technical strategy.
