PORT=8787
DATABASE_URL=postgres://earth:earth_dev_only@localhost:5432/earth
PG_POOL_SIZE=5

# Browser UI integration tests. Keep the password only in your ignored .env file
# or CI secret store; it must never be committed.
EARTH_TEST_EMAIL=vitalii.noga@gmail.com
EARTH_TEST_PASSWORD=
